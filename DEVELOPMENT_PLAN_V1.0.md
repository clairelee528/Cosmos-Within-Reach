# 咫尺星空（Cosmos Within Reach）V1.0 十天开发计划

> 计划版本：1.0  
> 开发周期：10 天  
> 依据文档：`PRD_V1.0_Cosmos_Within_Reach.md`、`ARCHITECTURE.md`  
> 核心目标：十天内完成可在真实投影环境演示的手势交互原型，并保留鼠标备用操作。

---

## 1. 最终交付目标

十天结束时，一名第一次体验的用户应当能够：

```text
进入太阳系总览
→ 用手势光标指向行星
→ 捏合选择行星
→ 旋转和缩放行星
→ 阅读行星知识
→ 张开手掌返回总览
```

同时满足：

- 水星、金星、地球、火星、木星、土星、天王星、海王星八大行星可交互。
- 鼠标可以完成与手势相同的完整流程。
- 摄像头或 MediaPipe 失败时，应用不会崩溃。
- 应用可以在 Chrome 或 Edge 中全屏运行并输出到投影仪。
- 至少完成一轮 3–5 人用户测试。
- 生产构建成功，没有阻断演示的控制台错误。
- 留下演示视频、截图、测试记录和作品集所需素材。

---

## 2. 开发优先级

### P0：必须完成

- Three.js 八大行星场景
- 完整鼠标交互闭环
- MediaPipe 单手追踪
- POINT、PINCH、OPEN_PALM 手势
- 手势光标、选择、旋转、缩放和返回
- 行星知识面板
- 摄像头和模型失败时的鼠标降级
- 投影全屏运行
- 调试模式、构建验证、用户测试和演示录制

### P1：时间允许时完成

- 四点空间校准及本地保存
- 更精细的加载、转场和首次使用引导
- 中英文切换
- 简单音效
- 更完善的视觉效果

### P2：1.0 后处理

- 精确 homography 或三维空间指向
- 高成本后期特效
- 精确轨道和天体比例
- 移动端适配
- 完整作品集长文排版

---

## 3. 每一步的协作方式

每个步骤完成后暂停并反馈以下内容：

1. **完成了什么**：用非技术语言解释本步骤的结果。
2. **修改了哪些文件**：说明每个主要文件的用途。
3. **现在能看到什么**：描述页面或交互的实际变化。
4. **如何验证**：提供尽量简单、可复制的操作步骤。
5. **是否存在偏差或风险**：说明与 PRD、架构或计划的差异。
6. **下一步做什么**：获得确认后继续当天后续步骤。

如果需要用户操作外部平台、浏览器权限、投影设备或 GitHub，将提供逐项操作说明，不默认用户熟悉命令行或开发工具。

每天结束时统一执行：

- 回归当天和前一天的主要功能。
- 检查浏览器控制台错误。
- 执行生产构建。
- 更新当天进度和已知问题。
- 展示本次 Git 变更摘要。
- 用户确认后提交并推送到 GitHub。

原则上每日一个主要提交；紧急修复可追加一个小提交。未经确认不覆盖用户已有改动，不使用破坏性 Git 操作。

---

## 4. 十天逐步执行计划

## Day 1：工程基础与可运行场景

**当天目标：** 浏览器中出现稳定运行的基础宇宙场景，并建立后续代码骨架。

### Step 1.1：创建项目脚手架

- 创建 Vite 原生 JavaScript 项目。
- 设置 `package.json` 和常用开发命令。
- 安装 Three.js 与 MediaPipe Tasks Vision。
- 添加 `.gitignore`，排除依赖、构建产物和本地文件。

**完成标志：** `npm run dev` 可以启动页面。

### Step 1.2：建立目录和核心配置

- 按架构建立 `app`、`config`、`scene`、`interaction`、`vision`、`gestures`、`ui`、`data`、`utils` 等目录。
- 创建应用状态、事件总线和基础配置。
- 暂时不实现手势逻辑。

**通俗解释：** 这一步像先把工作室分成不同房间，避免以后把摄像头、行星和界面代码全部堆在一起。

**完成标志：** 模块职责清楚，应用可以正常导入这些模块。

### Step 1.3：建立 Three.js 场景

- 创建 SceneManager。
- 创建场景、透视相机和 WebGLRenderer。
- 添加环境光、方向光和测试球体。
- 建立动画循环。

**完成标志：** 页面中央出现被正确照亮的球体。

### Step 1.4：基础页面和响应式处理

- 设置全屏深色页面。
- 处理浏览器尺寸变化。
- 限制设备像素比，避免投影电脑负载过高。
- 添加最基础的加载状态和 Debug 开关。

**完成标志：** 调整窗口大小后画面比例正常，没有拉伸或黑边异常。

### Step 1.5：Day 1 验收与提交

- 启动开发服务器进行人工检查。
- 执行 `npm run build`。
- 检查控制台。
- 提交信息建议：`chore: initialize project architecture`

---

## Day 2：八大行星与太阳系总览

**当天目标：** 建立具有基本视觉表现的八大行星总览。

### Step 2.1：建立行星数据

- 添加八大行星的尺寸、位置、速度、贴图路径和知识数据。
- 数据与 Three.js 渲染代码分离。
- 明确内容展示语言和单位。

**完成标志：** 修改数据即可改变行星内容，不必修改 UI 或场景逻辑。

### Step 2.2：实现 Planet 和 PlanetManager

- Planet 负责单颗行星的外观与动画。
- PlanetManager 负责创建、查询和更新全部行星。
- 为默认、悬停、选中和探索状态预留接口。

**完成标志：** 八大行星由统一数据自动创建。

### Step 2.3：完善行星视觉

- 添加适当分辨率的行星贴图。
- 制作土星环。
- 为贴图加载失败准备替代材质。
- 调整灯光和材质，确保投影下仍能看清。

**完成标志：** 八大行星容易辨认，土星环正常显示。

### Step 2.4：星空、布局与基础动画

- 添加轻量星空背景。
- 设置便于选择的非写实行星布局。
- 加入自转和轻量轨道运动。
- 控制 GPU 开销。

**完成标志：** 总览画面稳定、层次清楚，各行星不会频繁互相遮挡。

### Step 2.5：Day 2 验收与提交

- 检查八大行星、土星环、贴图失败降级和窗口缩放。
- 执行生产构建。
- 提交信息建议：`feat: build solar system scene`

---

## Day 3：完整鼠标交互闭环

**当天目标：** 不使用摄像头，也能完整体验产品。

### Step 3.1：光标和行星命中检测

- 监听鼠标移动。
- 使用 Three.js Raycaster 判断光标指向哪颗行星。
- 扩大必要的交互热区，避免小行星难以点击。

**完成标志：** 光标经过行星时能准确识别目标。

### Step 3.2：悬停反馈

- 添加轻微放大、光晕或亮度反馈。
- 显示行星名称。
- 平滑处理进入和离开悬停状态。

**完成标志：** 用户不需要说明也能感知当前目标。

### Step 3.3：选择与探索转场

- 点击选择当前悬停行星。
- 锁定选择，避免重复触发。
- 将目标行星移动或放大到主要位置。
- 淡化其他行星并进入 Explore 状态。

**完成标志：** 选择过程连续、不会卡在中间状态。

### Step 3.4：鼠标旋转、缩放和返回

- 鼠标拖拽旋转。
- 滚轮缩放并设置最小/最大范围。
- Escape 返回总览。
- 返回时恢复行星原始位置、大小和状态。

**完成标志：** 可以多次选择不同的行星而状态不混乱。

### Step 3.5：行星知识面板

- 显示名称、类型、直径、距离、周期、温度、描述和要点。
- 采用适合投影距离的大字号和短文本。
- 跟随 Explore 状态显示和隐藏。

**完成标志：** 投影观看距离下核心内容清楚可读。

### Step 3.6：Day 3 验收与提交

- 完整验证：总览 → 悬停 → 选择 → 旋转/缩放 → 阅读 → 返回。
- 构建并检查控制台。
- 提交信息建议：`feat: complete mouse interaction flow`

---

## Day 4：摄像头与 MediaPipe 手部追踪

**当天目标：** 稳定获得一只手的 21 个关键点，但暂时不控制行星。

### Step 4.1：摄像头权限和视频流

- 请求摄像头权限。
- 使用 640×480 作为初始分辨率。
- 处理允许、拒绝、设备不存在等情况。
- 摄像头失败时继续保留鼠标操作。

**可能需要用户操作：** 在浏览器弹窗中点击“允许使用摄像头”。届时会提供对应浏览器的操作说明。

### Step 4.2：初始化 MediaPipe

- 加载 Hand Landmarker 模型。
- 设置只追踪一只手。
- 管理模型加载状态和错误。

**完成标志：** 模型成功加载；失败时页面不崩溃。

### Step 4.3：实时追踪循环

- 将视频帧送入 MediaPipe。
- 防止上一次推理未完成时重复启动推理。
- 输出标准化 landmark、handedness 和时间戳。
- 在统一位置处理镜像坐标。

**完成标志：** 21 个关键点随手部平稳移动。

### Step 4.4：视觉调试和性能检查

- 添加可开关的摄像头/关键点调试层。
- 显示是否检测到手和基本 FPS。
- 测试手离开、返回和长时间运行。

**完成标志：** 丢手和重新识别不会让应用进入错误状态。

### Step 4.5：Day 4 验收与提交

- 验证摄像头允许、拒绝、丢手和重新出现。
- 验证鼠标流程没有回归。
- 提交信息建议：`feat: add mediapipe hand tracking`

---

## Day 5：手势识别与稳定处理

**当天目标：** 将关键点转换成稳定、可调试的 POINT、PINCH 和 OPEN_PALM。

### Step 5.1：手势基础工具

- 定义 21 个手部关键点常量。
- 实现距离、手指伸展和运动量计算。
- 将阈值放在配置文件中。

### Step 5.2：三个手势检测器

- POINT：食指伸出，其他主要手指收起。
- PINCH：拇指尖和食指尖接近。
- OPEN_PALM：四指或五指自然伸展。

**完成标志：** 调试面板能够显示当前原始手势。

### Step 5.3：稳定器与冷却

- 多帧确认重要手势。
- 对进入和退出使用适当阈值，减少临界点闪烁。
- SELECT 和 BACK 增加冷却。

**通俗解释：** 系统不会因为某一帧“看错了”就立刻选择或退出，而是要连续看对几帧才确认。

### Step 5.4：光标与移动平滑

- 平滑食指坐标。
- 计算手部水平和垂直移动量。
- 输出 pinch distance。
- 更新调试面板。

**完成标志：** 手保持不动时，数值和光标不会剧烈跳动。

### Step 5.5：Day 5 验收与提交

- 在不同距离、左右手和常见角度下测试手势。
- 记录当前最可靠的使用范围。
- 提交信息建议：`feat: add stabilized gesture engine`

---

## Day 6：手势光标、悬停与选择

**当天目标：** 用户可以不用鼠标选中一颗行星。

### Step 6.1：手势光标 UI

- 将食指坐标映射到页面。
- 创建默认、悬停、捏合三个视觉状态。
- 没有检测到手时隐藏光标。

### Step 6.2：手势光标接入命中检测

- 将手势与鼠标统一转换为光标事件。
- 复用 Day 3 的 Raycaster 和 hover 逻辑。
- 调整投影条件下的命中容错。

**完成标志：** 用手指可以移动光标并让目标行星产生 hover。

### Step 6.3：PINCH 选择

- 只有存在 hovered planet 时才允许选择。
- 使用确认后的 PINCH 触发 SELECT。
- 防止持续捏合导致重复选择。
- 进入转场后暂时锁定输入。

### Step 6.4：可靠性调优

- 连续完成至少 10 次指向和选择。
- 记录失败原因并调整平滑、阈值或热区。
- 快速邀请一名非开发者试用。

**完成标志：** 10 次操作中至少 8 次可以顺利选中预期行星。

### Step 6.5：Day 6 验收与提交

- 验证手势选择和鼠标选择都可用。
- 执行构建和回归测试。
- 提交信息建议：`feat: add gesture cursor and selection`

---

## Day 7：手势探索与完整无鼠标闭环

**当天目标：** 完整主流程可以只用一只手完成。

### Step 7.1：手势旋转

- Explore 模式中使用相对移动量旋转行星。
- 应用平滑和灵敏度限制。
- 防止刚进入 Explore 时行星突然跳转。

### Step 7.2：捏合缩放会话

- 捏合开始时记录基准距离和当前缩放。
- 保持捏合时使用距离变化控制缩放。
- 松开后结束缩放。
- 对缩放范围做限制。

### Step 7.3：旋转与缩放互斥

- 普通移动用于旋转。
- 捏合期间优先缩放，避免同时旋转。
- 丢手后立即停止连续操作，但不退出 Explore。

### Step 7.4：OPEN_PALM 返回

- 连续确认张开手掌。
- 增加进入 Explore 后的保护时间和返回冷却。
- 显示返回正在被确认的视觉反馈。
- 平滑恢复总览。

### Step 7.5：完整闭环压力测试

- 连续完成多颗行星的选择、旋转、缩放和返回。
- 让一名非开发者快速测试。
- 修复误选、误退和状态卡死。

### Step 7.6：Day 7 功能冻结与提交

- 核心功能冻结，不再新增主要手势。
- 执行完整构建和回归。
- 提交信息建议：`feat: complete gesture explore flow`

---

## Day 8：真实投影、校准与演示体验

**当天目标：** 产品从电脑屏幕原型转变为真实空间演示。

### Step 8.1：投影环境搭建

- 连接投影仪并设置扩展或镜像屏幕。
- 将浏览器窗口移动到投影画面并全屏。
- 固定摄像头、投影仪和主要用户站位。

**需要用户操作：** 连接真实硬件、调整投影方向和允许浏览器全屏。届时会根据 macOS 和投影连接方式提供逐项说明。

### Step 8.2：投影可读性调整

- 调整字体、对比度、行星亮度、标签和光标尺寸。
- 检查真实观看距离。
- 调整行星间距和交互热区。

### Step 8.3：基础四点校准

- 显示左上、右上、右下、左下四个目标。
- 采集对应的摄像头坐标。
- 实现基础范围映射。
- 支持跳过和重置。

### Step 8.4：校准保存与降级

- 尽量用浏览器本地存储保存本机校准结果。
- 校准失败时返回直接映射。
- 如果基础映射已经够用，不在此阶段冒险重写复杂算法。

### Step 8.5：加载、权限和操作引导

- 优化加载提示和摄像头提示。
- 用极短文案介绍指向、捏合和张手返回。
- 增加正式演示模式，隐藏开发调试信息。

### Step 8.6：Day 8 验收与提交

- 在真实投影环境执行完整流程。
- 记录设备位置和推荐站位。
- 提交信息建议：`feat: add projection calibration and polish`

---

## Day 9：3–5 人用户测试与集中修复

**当天目标：** 用真实用户验证交互是否容易理解，并修复阻断体验的问题。

### Step 9.1：准备测试脚本

- 统一测试任务和问题。
- 准备成功率、完成时间、误触和疲劳记录表。
- 不提前解释具体操作，先观察用户是否能自行理解。

### Step 9.2：执行 3–5 人测试

每位参与者完成：

1. 找到并选择土星。
2. 旋转土星。
3. 缩放土星。
4. 阅读一条知识信息。
5. 返回总览。
6. 选择另一颗行星。

**需要用户操作：** 邀请参与者、安排现场和确认是否允许拍摄。会提供主持话术和记录模板。

### Step 9.3：整理发现并分级

- P0：无法开始、无法选择、卡死、频繁误退、内容不可读。
- P1：操作不够顺畅，但可以完成任务。
- P2：视觉偏好或非关键建议。

### Step 9.4：只修复高优先级问题

- 调整手势阈值、光标平滑和交互热区。
- 修复状态、性能和 UI 可读性问题。
- 不引入新的手势或大范围视觉重构。

### Step 9.5：回归测试与提交

- 按相同脚本重新验证修复。
- 汇总测试结果和保留问题。
- 提交信息建议：`fix: improve interaction from user testing`

---

## Day 10：发布候选版、录制与交付

**当天目标：** 形成可复现、可录制、可展示的 V1.0 候选版本。

### Step 10.1：完整回归测试

- 测试手势和鼠标完整闭环。
- 测试摄像头拒绝、丢手、模型失败和纹理失败。
- 测试多次进入和退出 Explore。
- 检查控制台和性能。

### Step 10.2：生产构建与运行说明

- 执行最终生产构建。
- 创建 README。
- 写明安装、启动、构建、摄像头权限和投影设置。
- 记录建议浏览器、硬件摆放和已知问题。

### Step 10.3：录制演示视频

- 准备固定的演示路线。
- 录制用户、投影画面和关键手势。
- 补录干净的屏幕画面。
- 保存可用于作品集的高质量截图。

**需要用户操作：** 布置拍摄环境和完成真人手势演示；会提前提供镜头清单。

### Step 10.4：整理作品集素材包

- 保存架构图、状态流、手势词汇、校准过程和测试结果。
- 建立案例结构草稿。
- 标记仍需后续制作的内容。

### Step 10.5：最终验收

最终现场脚本：

```text
启动应用
→ 允许摄像头
→ 完成或跳过校准
→ 指向土星
→ 捏合选择
→ 旋转和缩放
→ 阅读知识
→ 张开手掌返回
→ 选择第二颗行星
→ 完成演示
```

### Step 10.6：发布候选版提交

- 确认最终变更摘要和已知问题。
- 提交信息建议：`chore: prepare v1 release candidate`
- 推送 GitHub。
- 可选：创建 `v1.0.0-rc1` 标签。

---

## 5. 每日完成定义

一天的任务只有在以下条件满足后才算完成：

- 当天计划中的 P0 步骤已经完成。
- 先前已完成的核心功能没有回归。
- 开发服务器能够启动。
- 生产构建成功。
- 没有新的阻断性控制台错误。
- 变更已经解释并获得确认。
- Git 提交和推送成功，或已明确记录无法推送的原因。

---

## 6. 进度偏差处理

如果某一天出现延误：

1. 首先取消当天的视觉增强。
2. 其次将 P1 功能移到 Day 8 或 V1.1。
3. 不削减鼠标备用操作、错误降级和构建验证。
4. 不为了赶进度破坏模块边界。
5. Day 7 后不再新增主要功能，只修复和打磨。

如果四点校准不够准确，V1.0 优先采用以下组合保证可用性：

- 固定摄像头和用户站位；
- 扩大行星交互热区；
- 增强 hover 反馈；
- 保留鼠标演示模式；
- 将精确空间映射列入 V1.1。

---

## 7. 用户需要参与的时间点

预计需要用户明确参与的环节：

- **首次运行：** 浏览器摄像头授权。
- **每天结束：** 确认当天结果后允许提交和推送。
- **Day 6–7：** 简短试用手势交互并反馈体感。
- **Day 8：** 连接和摆放投影仪、摄像头，配合实机校准。
- **Day 9：** 邀请 3–5 名测试参与者。
- **Day 10：** 配合真人演示视频拍摄。

每次需要参与时，将单独提供：准备事项、点击路径、操作步骤、预期结果和常见问题处理方式。

---

## 8. GitHub 工作方式

远端仓库：`https://github.com/clairelee528/Cosmos-Within-Reach.git`

推荐采用单一 `main` 分支完成十天 MVP，每天形成一个稳定检查点：

```text
完成当日步骤
→ 本地测试
→ 生产构建
→ 查看 Git 变更
→ 用户确认
→ 提交 commit
→ 推送 GitHub
```

如果远端仓库已有 README 或其他初始化文件，首次推送前先安全拉取并检查差异，不覆盖远端内容。
